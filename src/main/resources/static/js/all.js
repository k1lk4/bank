/*
let firstname_validation = document.getElementById('firstname_validation');

function validateNewCard() {
    firstname_validation.innerHTML = validateFirstname();
    if (validateFirstname() === true) {

    }
}

function validateFirstname() {
    let firstname = document.getElementById('firstname').value;

    if (firstname === null || firstname === "") {
        return "Имя не может быть пустым";
    } else if (firstname.length < 2 || firstname.length > 20) {
        return "От 2 до 20 символов";
    } else {
        firstname_validation.style.backgroundColor = "#ff0000";
        return true;
    }
}

*/
